# Remote listener forwarding

This is the implementation contract for the outstanding remote-forwarding parity row. Listener discovery and an initial automatic forwarding implementation are present; runtime verification and the full gates below remain outstanding. Reuse the established workspace SSH transport and the daemon's `proxy.open`, `proxy.stream.subscribe`, `proxy.write`, and `proxy.close` operations. Do not create a second SSH process for every listener.

## Ownership

A forwarding supervisor belongs to one `run_proxy_routing` connection generation. It consumes validated current `SshBridge.listeners` observations. Maintain at most sixteen advertised local listeners per connection and sixteen accepted client streams in total. Local binds use IPv4 loopback only, prefer the remote port when available, and otherwise request an ephemeral local port. Publish the actual endpoint with remote address, port and originating workspace/surface identity. Two workspaces exposing the same remote port must remain distinguishable.

Each listener owns its accept loop and clients. Service disappearance stops accepting immediately and asks clients to close. Connection teardown cancels the supervisor and all client work; closing SSH stdin/transport causes the owned daemon to close every registered stream. Clear endpoint publication before a new connection generation is advertised. Never reuse a prior connection's forwarded endpoint as proof of current routing.

A client opens a remote proxy stream with the discovered destination, installs response routing before subscribing, and copies bytes in both directions. Map unspecified remote binds to the same address family's loopback destination. Preserve explicit remote bind addresses. Validation permits only destinations drawn from the current owned-listener observation, rather than arbitrary client-supplied proxy targets.

## Reader and backpressure contract

The shared SSH stdout reader carries terminal output, proxy data and RPC replies. It must not await a forwarded client's network write or a full client queue: a client may itself be waiting for an RPC reply later in that same reader. Route proxy data through per-client queues bounded to sixteen 32-KiB chunks. A queue overflow retires that client stream with an explicit diagnostic; do not silently discard bytes and continue a corrupted connection. Terminal output retains its existing backpressure behavior.

Keep client data routes separate from terminal `stream_to_pane` routes. Remove a route on EOF/error before notifying the consumer. Bound encoded proxy frames before decoding; the owned Go pump emits 32-KiB chunks. Client writes use the existing bounded correlated RPC transport. Connection count limits plus fixed copy buffers bound aggregate retained data.

## Cancellation and errors

`proxy.open` can mutate the daemon even when a local caller stops waiting. Listener removal must signal cancellation and let an in-flight open finish within the existing RPC deadline, then close any returned stream. Do not abort an open and forget a possible remote stream while retaining the same connection indefinitely. Once a stream ID is known, one owner issues its bounded close and removes local routing. Full connection teardown may abort that owner because daemon teardown is then authoritative cleanup.

Validate response stream IDs and generation before publishing success. Unknown, duplicate or replaced identities fail closed. No GTK borrow or bridge mutex may span network I/O. Listener/client tasks are tracked and reaped; no unbounded detached task or close-future registry.

## Browser routing

Forwarded endpoints support tools and ordinary local clients. Browser workspace-origin parity additionally requires all page subresources, redirects and WebSockets to follow the selected remote workspace; rewriting only the initial localhost URL cannot satisfy that requirement. Extend each surface-owned browser startup with a workspace-owned proxy configuration or a complete request routing adapter. Audit localhost bypass rules explicitly and preserve independent browser daemon identity. Do not mark browser remote-origin parity complete on evidence from a single rewritten document request.

## Verification gates

Actions must exercise a real remote HTTP server through the established SSH daemon, response bodies larger than one proxy chunk, simultaneous local clients and unrelated terminal input. Two workspaces must expose the same remote port with independent endpoint identity. Verify listener removal, active-client closure, delayed-open cancellation, reconnect invalidation, bounded overload and absence of retained daemon streams. Record setup latency, byte counts, failures and active listener/client counts without payloads. Browser tests must include remote-only subresources and a WebSocket, not merely a document title.

The desktop and remote sides already share bounded RPC identity/tracing; use these existing boundaries for forwarding diagnostics. Current sources are `src/ssh/tunnel.rs`, `src/ssh/bridge.rs`, `src/ssh/writer.rs`, `daemon/remote/cmd/cmuxd-remote/streams.go` and the [ports gateway context](../.agent/api/cmux-ports.yaml).

## Initial implementation checkpoint

`src/ssh/forward.rs` implements connection-owned discovery reconciliation, sixteen listeners/clients, at most thirty-two active or retiring listener tasks, bounded proxy queues and loopback endpoint publication. Port records expose nullable `forwarded_local_port`. Client retirement uses the established bounded outbound control channel for remote close; full connection teardown remains the authoritative cleanup fallback. An in-flight open settles before service-stop cancellation is applied. The real SSH fixture now transfers a payload larger than one proxy chunk through the published endpoint.

Runtime verification is pending. Remaining gaps include TCP half-close fidelity, multiple-workspace collision/reconnect and overload workload evidence, aggregate byte/active counters, and full browser origin/subresource routing. These are required follow-ups rather than claims covered by the initial fixture.

Forwarding route registration now rejects duplicates atomically without changing the existing owner. Capacity rejection closes the newly opened remote stream. Listener termination signals its client stop channel and removes only its matching endpoint before draining pending opens. The supervisor removes completed listener registrations and can recreate them while the service remains advertised. A route test verifies duplicate-owner preservation and explicit overload retirement; runtime verification pending.

Client-to-server half-close now uses daemon `proxy.shutdown_write`: local request EOF sends TCP FIN while retaining the response reader. The daemon accepts registered TCP connections only and leaves stream ownership intact. A real TCP Go test and the multi-chunk SSH fixture require response delivery after request EOF. Reverse-direction half-close behavior and compatibility with older daemons remain separate verification work; the existing daemon stream pump still retires a connection on remote read EOF.

Normal client completion and subscription failure now await an idempotent `proxy.close` response through the bounded request transport. Only `closed: true` confirms retirement. The owner remains live during that await, so cancellation or failure still queues the existing close fallback; connection teardown remains authoritative cleanup for interrupted transport. Diagnostics distinguish requested, confirmed and failed closes. The SSH fixture requires a confirmed close and no close failure after the transferred response; runtime evidence pending.

Forwarding subscriptions now opt into `half_close: true`. For TCP streams the daemon emits `proxy.stream.read_eof` on clean read EOF and retains the writable connection until explicit close. Existing subscriptions keep their previous full-retirement behavior. The desktop forwards FIN to the local response direction and waits for both transfer directions to finish, while errors or cancellation retire the connection. Each route reserves one termination slot in addition to sixteen data slots, preserving ordered EOF/error delivery when data capacity is full. Go real-TCP coverage verifies late writes after remote FIN; a Rust route test verifies EOF ordering behind sixteen chunks. The real SSH fixture also reads an early response through FIN before sending a multi-chunk request, checks the remote payload, acknowledged byte counts, confirmed close and unchanged workspace selection. Runtime verification of that new scenario and older-daemon capability negotiation remain outstanding.

## Connection capability negotiation

The validated hello now returns optional feature support with its handler timing. Listener polling requires `ports.list`; forwarding additionally requires `proxy.shutdown_write` and `proxy.stream.half_close`. A terminal-capable older daemon remains connected without exposing forwarding endpoints whose FIN semantics it cannot preserve. Upgrade the remote daemon and reconnect to enable missing features. Every connection negotiates afresh; handshake diagnostics expose `ports_supported` and `forwarding_supported`. Handshake transport tests cover full, partial and absent capability sets. Strict workspace linting passes; Actions runtime evidence is pending.

The SSH fixture now also closes the remote listening socket while retaining an accepted idle connection. It requires discovery-driven endpoint removal, EOF at both accepted peers, an acknowledged remote close, zero listener/client tasks and unchanged workspace selection before continuing terminal operations. The benchmark artifact records retirement latency and counters. Because the fixture uses loopback SSH in the same network namespace, it also checks that the occupied preferred port produces a distinct published fallback port. This covers a local bind collision; independent same-port workspaces and reconnect invalidation still need separate evidence. Syntax checks pass; runtime verification is pending Actions.

## Browser origin implementation plan

Source inspection confirms `BrowserManager::startup_async` centralizes both preview and RPC startup, and `browser::cli::start` is the launch boundary for the private daemon. The installed agent-browser public CLI exposes `--proxy` and `--proxy-bypass`; it also inherits standard proxy environment variables. Remote startup must supply explicit values at this shared boundary so inherited local bypass settings cannot redirect a remote page locally.

Chromium SOCKS5 resolves target hostnames at the proxy and supports TCP URL traffic. WebSockets use the configured “other proxies” list. Its implicit localhost bypass must be removed with `<-loopback>`; PAC settings cannot remove that implicit bypass. Use a single manual SOCKS5 proxy without a direct fallback. These transport facts come from [Chromium proxy documentation](https://chromium.googlesource.com/chromium/src/+/main/net/docs/proxy.md). This is a TCP web-origin design, not a UDP/WebRTC tunnel.

The next implementation should have these ownership boundaries:

1. A workspace-owned loopback SOCKS5 listener retains its bound socket across SSH reconnects. Holding the socket prevents a stale browser proxy port from being reassigned to another process. Closing the workspace retires that listener and its browser sessions together. Disconnected requests fail explicitly; they never fall back to local networking.
2. Each accepted request captures the currently negotiated SSH generation. Use a bounded admission channel to the connection-owned forwarding worker, preserving the existing sixteen-client capacity and shared-reader queue bounds. Generation teardown retires in-flight streams; a later request may use the new generation, but an existing TCP stream is never replayed automatically.
3. Share remote open/subscribe/copy/half-close/acknowledged-close ownership with discovered-port forwarding. The SOCKS adapter supplies a validated hostname or IP plus nonzero port; DNS stays in the remote daemon's existing `net.DialTimeout`. Keep this browser destination path separate from discovered-port listeners, whose destinations remain restricted to observed services.
4. SOCKS negotiation accepts CONNECT only, bounds the greeting and domain fields by their protocol byte lengths, and uses a fixed handshake deadline. Reply success only after remote open and stream subscription succeed. Unsupported authentication, UDP/BIND, malformed address and overload receive protocol failure and close. Never hold a bridge mutex while awaiting negotiation or RPCs.
5. Browser managers capture the owning workspace proxy at creation, including suspended-tab initialization and RPC opens into a background workspace. Existing browser history and DOM survive SSH reconnect because the local proxy endpoint stays stable. Startup must wait boundedly for proxy readiness or return an explicit error; it must not launch a remote workspace page with local defaults.

Actions must place the remote HTTP fixture in a different network namespace or otherwise prove the local endpoint cannot serve it. Cover relative scripts, absolute localhost subresources, remote DNS, redirects and WebSocket echo; serve distinguishable same-port pages in two workspaces; disconnect while a request is live, then reconnect and navigate using the same browser identity. Inspect actual subprocess launch arguments for loopback bypass configuration and assert no local fallback request was received. Record proxy admission/handshake outcomes, generation identity, active tasks and byte counts without URLs or payloads. This section is an implementation plan; browser origin routing is still absent.

## SOCKS transport foundation

`src/ssh/socks.rs` now creates a loopback endpoint retained by the workspace bridge across SSH generations. Its accept task belongs to the SSH lifecycle; the bridge retains the bound listener even if retry exhaustion stops accepting, preventing stale port reassignment while that bridge remains alive. At most sixteen negotiation tasks parse bounded SOCKS5 no-auth CONNECT fields under five seconds. Each captures the generation sender at accept and enters a sixteen-slot channel; disconnected/full admission fails. The connection worker shares its sixteen transfer permits with discovered-port clients and reaps completed tasks. Remote open, subscription, data routing, half-close and acknowledged retirement use the same transfer implementation. Success is sent only after subscription; hostnames go directly to remote dialing. Protocol tests cover unsupported authentication and preservation of application bytes after a hostname CONNECT. Strict workspace Clippy passes; runtime execution is pending Actions. Browser startup is not yet wired, and stable-endpoint reconnect/overload and actual remote web coverage remain required.

## Browser startup wiring

BrowserManager::for_workspace now captures the remote bridge for UI opens, background RPC opens and suspended-tab initialization. The manager retains it through its bounded shutdown future. Startup waits up to five seconds for current proxy admission within the existing fifteen-second transaction, then passes `--proxy socks5://127.0.0.1:PORT --proxy-bypass <-loopback>` to the private public-CLI launch. Missing bridge or unavailable support fails explicitly; local workspaces keep their existing launch settings. A child-process fixture checks the actual launch argument boundaries. Strict Clippy passes; real remote subresources/WebSockets and reconnect identity still require Actions coverage. A failed daemon-close deadline still does not prove daemon termination.

## Isolated remote browser scenario

Actions now reruns the real SSH workspace fixture with each daemon and its PTYs inside a fresh network namespace containing only loopback. A local decoy occupies the same port as the remote HTTP service. The background-workspace browser must follow a redirect, load relative and absolute localhost scripts, fetch an absolute loopback resource and receive a WebSocket message from the remote service; any decoy request fails the scenario. The report records resource readiness duration, checked paths and the bounded decoy request count. The external browser tool remains pinned by the existing Actions installation step. Python and embedded server syntax validation pass; execution is pending Actions. Remote DNS beyond localhost, multi-workspace same-port identity, live reconnect and overload coverage remain required.

The isolated browser fixture now starts distinguishable same-port services in two separate remote workspace namespaces. Both pages must retain their own script identity, including after navigating the first again; their discovered forwarded endpoints must be distinct and avoid the occupied local decoy port. Both browser surfaces are explicitly retired through an exit stack even when one cleanup fails. Syntax validation passes; Actions runtime evidence is pending. Live SSH reconnect and remote DNS beyond localhost remain separate gates.

## Reconnect observability and scenario

Workspace list/current JSON now exposes nullable `remote`: connection_state, nullable reconnect_attempt, retained browser_proxy_port and browser_proxy_ready. Port retention does not imply readiness. These live observations are transient and independently sampled from GTK and the bridge.

The isolated remote browser scenario now terminates the first fixture daemon through its owned PTY shell, waits for a newly traced generation subscription, verifies the original browser DOM remains alive, and requires a fetch to fail while the new namespace has no service. After restarting the remote HTTP service, the same browser must load all resource identities again using the unchanged proxy port; the second workspace must still serve its own content and the local decoy must receive no traffic. The report adds reconnect duration and before/after transport state. Strict Clippy and Python syntax validation pass; Actions execution remains pending. This scenario tests service absence following reconnect, not a deterministic observation of every intermediate disconnected state.

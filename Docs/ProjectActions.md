# Project configuration and actions

The active parity goal includes project actions, workspace definitions, command execution, palette discovery and configurable UI actions. The first implementation provides read-only resolution through `cmux project-actions --directory PATH` (JSON output, no running app required). Explicit local command execution is available through `cmux project-run ACTION --workspace UUID --fingerprint HASH`; discovery never executes commands.

The resolver reads global `$XDG_CONFIG_HOME/cmux/cmux.json` (falling back to `$HOME/.config/cmux/cmux.json`) and searches upward from the canonical requested directory for the nearest project file. At each directory `.cmux/cmux.json` wins over `cmux.json`. The winning project action replaces the entire global entry with the same ID; unrelated global actions remain. Each returned action retains its complete raw definition and winning source path.

This matches `findCmuxConfig`, `loadAll`, and `mergedActionEntries` in the inspected upstream `Sources/CmuxConfig.swift` at e36b8e8632a414e2982185f8dae4002a98be2b53. That implementation uses nearest-project action resolution plus global fallback; its separate notification-hook lookup walks the ancestor hierarchy. The directory-actions dogfood README describes broader ancestor inheritance, so do not infer action merging from that README alone.

Reads require regular files, follow symlinks and use the platform nonblocking-open helper to avoid waiting on FIFO peers. Each file is limited to 256 KiB; lookup visits at most 64 directory ancestors; the final registry contains at most 256 actions with nonempty IDs bounded to 128 bytes. Invalid selected files produce errors instead of silently running a lower-precedence file. JSON structure and the typed action schema are validated. Other configuration sections are not yet applied.

Next steps are typed command/builtin/agent/workspace/workspaceCommand definitions and explicit target semantics, shared GTK-worker/RPC resolution, reviewable trust for project-defined execution, palette integration, action execution and launch-layout support. Existing launch/resume ownership should be reused. Merely entering a directory or listing actions must never execute project commands. Remote project files require remote reads through their workspace transport; local filesystem resolution is not a substitute.

Actions coverage includes actual directory precedence and source provenance. Strict workspace Clippy passes; runtime execution remains in GitHub Actions. Generated command completions and the man page follow the shared CLI schema.

Action records now include typed `intent` and `target` alongside the raw definition. Command, agent, builtin, workspaceCommand, workspace and metadata-only families use upstream explicit-type/inference order. Command text remains literal; required executable strings reject blank/NUL/oversize values. Targets accept currentTerminal and newTabInCurrentPane, defaulting to a new sibling tab. Presentation strings, booleans and keyword arrays have explicit type/size checks. Workspace payloads remain raw objects and builtin names are not yet resolved to the Linux capability catalog; those are not validated execution plans. Tests cover literal command preservation, explicit-type precedence, targets and malformed executable fields. Strict Clippy passes; Actions runtime results are pending.

Builtin intent now uses a closed enum with upstream canonical IDs and spelling aliases, rejecting unknown names. The catalog retains agent-chat, cloud, mobile and simulator identities alongside workspace/terminal/browser/split actions. Recognition is not an execution-availability claim: handlers and platform adaptations remain outstanding. Alias/unknown-name tests compile under strict Clippy; Actions runtime execution is pending.

Workspace intent now contains typed name/cwd/color/setup/environment and recursive layout data. Layouts accept either a pane with at least one surface or a horizontal/vertical split with exactly two children, rejecting mixed pane/direction nodes. Ratios default to 0.5 and clamp to 0.1–0.9 like upstream. Validation bounds Linux depth to16 and total surfaces to128; each environment has at most64 identifier keys and64 KiB of bounded NUL-free values. Surface terminal/browser/project types, optional focus booleans and launch strings are validated. Project-surface rendering, named-color resolution and restart behavior remain execution gates. Strict Clippy passes; Actions tests cover mixed layouts, ratio behavior and invalid nodes/environment.

`cmux project-actions --workspace UUID --json` now calls `project.actions.list`. An omitted RPC workspace selects the current workspace; CLI inspection without --workspace remains local/offline, and --directory conflicts with --workspace. GTK captures the target UUID and local terminal CWD through the same helper used by Git metadata. Two admitted blocking readers share a five-second response deadline; a started filesystem read retains its permit on timeout/cancellation and may outlive the response. Results describe the captured workspace/directory rather than silently switching targets if focus or CWD changes. Remote reads remain explicitly unsupported. Resolution emits trace/workspace/duration/outcome without config contents. Actions coverage checks background lookup, no command execution, source changes after terminal cd and unchanged focus. Strict Clippy and Python syntax checks pass; runtime verification is pending.

Resolved actions now include a SHA-256 fingerprint over a versioned tuple of captured directory, source path, action ID and complete raw definition. This lets the forthcoming execution path reject a changed action or execution context after inspection. It is a change detector, not an authorization credential, stored trust or permission to execute. Tests verify command/target/source/ID/directory changes invalidate it. Strict Clippy passes; Actions execution is pending.

Explicit command execution now re-reads configuration off GTK under the shared two-reader admission limit and five-second deadline. `project.actions.run` requires action_id and fingerprint, accepts an optional workspace_id (default current), and rejects changed definitions, directory context or selected surface before submitting input. CLI callers explicitly request execution; fingerprints provide change detection rather than persistent trust. Only local command intents are executable in this checkpoint.

The default newTabInCurrentPane target opens a selected sibling terminal in the captured canonical directory. A separate `/bin/sh -c` evaluates the command unchanged, then an interactive shell remains; subsequent ordinary tabs retain their original launch defaults. currentTerminal submits literal text and a carriage return to the existing terminal. Execution selects its target workspace. The response contains workspace_id, surface_id and status=submitted, which does not report process completion or exit status. A payload-free project.actions.run event records trace, workspace, surface and submission duration. Config inspection and failed fingerprint checks do not select or launch anything.

Actions coverage now checks stale-fingerprint rejection with no file or focus effects, followed by successful new-tab execution in the reviewed directory. Local Clippy and syntax checks pass; runtime verification remains pending Actions. Persistent trust, palette confirmation, other action families, remote configuration and workspace layout application remain within the active parity goal.

Local builtin execution now supports cmux.newTerminal, cmux.splitRight and cmux.splitDown through the existing SplitEngine tab/split operations. These actions share fingerprint/context checks and submission tracing; their named operation determines layout rather than the terminal-command target field. They select the resulting terminal, preserve ordinary workspace launch policy and save the session. Other builtin families remain explicitly unsupported pending their integrations. Actions coverage creates and closes each builtin surface, checking identity, selection and surface count; runtime pending.

The cmux.newWorkspace builtin now creates a selected local workspace bound to the reviewed canonical directory, using existing workspace creation and sidebar wiring. Run responses include source_workspace_id and workspace_id (the destination), plus its selected surface_id. The source workspace remains intact. Actions coverage checks distinct identity, directory, selection, count and normal workspace cleanup. Runtime verification is pending.

The cmux.newBrowser builtin opens about:blank through the existing asynchronous browser lifecycle. Its fingerprint is checked before startup; if the captured selected surface changes during startup, no browser tab is installed. Successful project execution selects the created browser, while ordinary browser.open retains its existing no-focus behavior. Caller cancellation uses the existing startup owner/abort cleanup. project-run now has a thirty-second client deadline to accommodate configuration resolution and browser startup. The mock-browser Actions fixture checks selected workspace/surface and isolated daemon retirement. Runtime pending.

Failure-path Actions coverage now pauses project browser navigation, changes the target pane, selects an observer workspace, then releases startup. It requires a changed-context error, identical pre/post surface and focus records, and retirement of the rejected startup daemon without retiring prior sessions. This exercises the shared startup-owner cleanup rather than only successful creation. Python syntax passes; runtime pending Actions.

Agent actions now execute through the same reviewed command targets. Names are trimmed; claudeCode/claude-code map to claude and openCode/open-code map to opencode. Args are trimmed shell text, preserving quoting and expansion, matching CmuxConfigAction.terminalCommand at the inspected upstream pin. Existing CLI-name validation allows ASCII letters/digits/dot/underscore/hyphen; arbitrary command paths or Unicode executable names remain unsupported. Agents are discovered through the terminal shell, not installed automatically. Tests cover aliases and quoted args; the GTK fixture executes a harmless custom printf agent and checks exact output in the project directory. Runtime pending Actions.

Resolved config now exposes a named commands registry with winning source, raw definition, typed intent and fingerprint. Commands use the same global-plus-nearest-file selection, local whole-entry precedence and first occurrence of a duplicate name within one file, matching inspected upstream loadAll. Names are nonblank/control-free and bounded to128 bytes; arrays and merged registries are bounded to256 entries. Workspace-command action fingerprints incorporate the winning referenced definition and source (or its absence), so indirect changes invalidate review. Named workspace execution, restart semantics and complete command-specific validation remain outstanding; discovery alone does not execute them. Tests cover precedence, duplicate names and indirect fingerprint invalidation; runtime pending Actions.

Named definitions now use a dedicated schema adapter: exactly one non-null workspace or command is required; blank commands fail; restart accepts only new/recreate/ignore/confirm. Description, keywords and confirm receive existing bounded validation. Action-only type/agent/builtin/target fields cannot change named-command intent. Every duplicate is validated before first-name precedence is applied, matching decoding before upstream loadAll. Tests cover ambiguity, invalid restart/confirm and action-field isolation. Restart behavior is validated but not yet applied at launch; runtime pending Actions.

The native terminal adapter now accepts validated local launch environments separately from shell command text. SplitEngine retains workspace overrides for new tabs/splits; native initialization merges inherited values, then explicit overrides, then fresh cmux routing identity. CMUX_ overrides are filtered case-insensitively. Strings are owned through ghostty_surface_new, and no process-global environment is mutated. The native Actions fixture checks a literal dollar-sign/space value and rejection of a forged surface ID. Workspace action application and persistence of these launch settings remain outstanding; this is the native prerequisite, not a complete layout launch.

Workspace session snapshots now retain explicit launch_environment maps from SplitEngine. Restored local terminals and later tabs/splits receive these overrides, with native managed-identity precedence unchanged. Older snapshots default empty; deserialization shares project environment validation (64 entries, key/value and aggregate limits), rejecting malformed persisted values. This stores explicit overrides only, not ambient environment. Round-trip/backward-compatibility/malformed-map tests are included; Clippy passes, runtime pending. Project layout application remains open.

The multi-restart scrollback Actions fixture now seeds explicit launch overrides in a saved workspace, leaves it unopened across another normal quit, and verifies persistence plus child-shell values after restore. It also splits a fresh terminal and checks the same literal override with a new surface identity. A forged CMUX_SURFACE_ID in the saved map must never reach either child. Python syntax passes; runtime verification remains pending Actions.

Project validation and GTK session reconstruction share a 16-level Linux depth bound. The inspected upstream source has no explicit cap, but Actions run34062084578 showed a 17-deep GtkPaned tree could listen on the socket while the GTK main thread failed readiness for ten seconds. The session-loading fixture therefore exercises16 nested splits and requires all17 panes/surfaces and the original workspace name.

Inline workspace and named workspaceCommand actions now execute default-layout launches with name, cwd, RGB hex color and environment. Relative cwd resolves against the reviewed canonical project directory; directory/name/color preparation completes on the existing admitted blocking worker before GTK mutation. AppState creates the first terminal with overrides before realization, retains them for later terminals and persistence, selects the new workspace and wires its sidebar row. Responses retain source/destination identity. Layout/setup requests and named restart policies other than new are explicitly rejected; omitted restart currently creates new and full upstream restart semantics remain outstanding. Actions fixture exercises both inline and named launches with actual child environment output. Clippy/AST pass, runtime pending.

Default-layout workspace setup now submits literal input plus carriage return to the first freshly initialized terminal, matching upstream sendConfigSetupCommand input semantics. Pending setup text is consumed and released after one delivery attempt. It is absent from future-tab launch defaults and session snapshots, so ordinary restore does not repeat setup. workspace.setup.submit records pane/outcome without script text; submission does not imply process completion. Inline/named Actions fixtures check actual setup output using the configured environment. Clippy/AST pass; runtime pending for this change.

Custom workspace layouts now execute through the same project.actions.run path. Preparation runs off GTK, resolves every relative terminal directory against the reviewed workspace root, assigns stable surface identities and rejects unsupported project surfaces before allocating widgets. GTK installs the complete split tree directly, preserving ratio, pane-local selected tab and the last requested focused surface. Workspace setup is consumed by the first terminal and joined with that surface command; other terminal commands remain independent. Per-surface environment overrides win over workspace values and are bounded again during session deserialization. Browser surfaces use the shared lazy restored-tab lifecycle. Actions fixtures cover two-terminal execution and browser navigation/cleanup; runtime verification is pending.

Named workspace restart policies now follow upstream name-collision semantics. `new` creates another workspace, `ignore` selects the matching workspace without launching, and `recreate` constructs the replacement before closing the match. A failed replacement therefore leaves the existing workspace intact. `confirm` reports `confirmation_required` when a match exists; the pending command-palette integration must collect and pass the explicit decision. Actions coverage checks ignore and recreate identity/count behavior; runtime verification is pending.
